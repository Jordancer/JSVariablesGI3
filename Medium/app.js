var sharePizza = cutPizzaSlices(8);

function cutPizzaSlices(x) {
  return x;
}

function sharePizza(x) {
  return "each Person Gets " + sharePizza / x + " Slices of Pizza";
}

console.log(splitPizza(2));
